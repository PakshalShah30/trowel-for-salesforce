{"id": "dead-flow-01", "tier": 1, "question": "Which Active autolaunched Flows does nothing invoke?", "query": {"op": "detect", "rule": "TRW001"}, "expected": ["flow:Orphan_Notifier"], "guards": "The exact-list form is load-bearing: it fails both on a missed finding and on a record-triggered or Apex-invoked Flow being wrongly added."}
{"id": "dead-flow-02", "tier": 1, "question": "Is the Apex-invoked Flow correctly treated as alive?", "query": {"op": "inbound", "node": "flow:Apex_Invoked_Flow", "edge": "invokes"}, "expected": ["apex_class:LeadService"], "guards": "Drop Apex parsing and this returns [], which would make a live Flow look dead."}
{"id": "dead-flow-03", "tier": 1, "question": "Does a commented-out Flow.Interview call count as a caller?", "query": {"op": "inbound", "node": "flow:Orphan_Notifier"}, "expected": [], "guards": "LeadService.cls names Orphan_Notifier inside a comment block. If the comment stripper regresses this returns a caller and TRW001 goes silent."}
{"id": "dead-flow-04", "tier": 1, "question": "Is the subflow call recorded as a reference?", "query": {"op": "inbound", "node": "flow:Shared_Utility", "edge": "invokes"}, "expected": ["flow:Lead_Assignment"]}
{"id": "field-01", "tier": 1, "question": "Which custom fields have no reader or writer in scope?", "query": {"op": "detect", "rule": "TRW002"}, "expected": ["field:Lead.Legacy_Code__c"], "guards": "Score__c is written by Lead_Assignment. If it appears here, field-write parsing broke."}
{"id": "graph-01", "tier": 1, "question": "What would deleting Lead.Score__c affect?", "query": {"op": "impact", "node": "field:Lead.Score__c"}, "expected": ["flow:Lead_Assignment", "flow:Shared_Utility"], "match": "contains", "guards": "Shared_Utility is two hops away. A one-hop join finds only Lead_Assignment; this case is the reason the graph exists."}
{"id": "graph-02", "tier": 1, "question": "Which object does LeadTrigger bind to?", "query": {"op": "outbound", "node": "apex_trigger:LeadTrigger", "edge": "triggers_on"}, "expected": ["object:Lead"]}
{"id": "graph-03", "tier": 1, "question": "Which object does the record-triggered Flow bind to?", "query": {"op": "outbound", "node": "flow:Lead_Assignment", "edge": "triggers_on"}, "expected": ["object:Lead"], "guards": "This edge is what proves the Flow is platform-invoked and must never be reported dead."}
{"id": "catalog-01", "tier": 1, "question": "How many Flows are in the dig site?", "query": {"op": "count", "kind": "flow"}, "expected": 6}
{"id": "catalog-02", "tier": 1, "question": "Are both custom fields cataloged?", "query": {"op": "count", "kind": "field"}, "expected": 2}
{"id": "narrative-01", "tier": 2, "target": "fixtures/dig-site/flows/Lead_Assignment.flow-meta.xml", "question": "Explain what this Flow does and when it runs.", "rubric": "Score 1-5. A 5 states that the Flow is record-triggered on Lead (after save, on create and update), that it writes the Score__c field, and that it calls Shared_Utility as a subflow. Deduct for any object or field named that does not appear in the XML. Deduct for describing it as manually or scheduled invoked. A 3 gets the trigger right but misses the subflow or the field write. A 1 invents behaviour.", "min_score": 4}
{"id": "narrative-02", "tier": 2, "target": "fixtures/dig-site/flows/Orphan_Notifier.flow-meta.xml", "question": "Explain what this Flow does and when it runs.", "rubric": "Score 1-5. A 5 states that the Flow is autolaunched (so it must be invoked by name rather than by a record event) and that it queries Lead records. Credit for noting that nothing in the XML itself reveals a caller. Deduct heavily for claiming it is record-triggered, since the XML has no start element at all - that specific hallucination is the one this case exists to catch. A 1 invents a trigger or fields.", "min_score": 4}
