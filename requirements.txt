# Core pipeline (weeks 1-3): catalog, dependency graph, detectors, CLI.
# Deliberately small — running `detect` on the bundled fixtures should not
# pull a 2GB deep-learning stack.
networkx>=3.3
rich>=13.0
pytest>=8.0
